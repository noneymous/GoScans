checks = [
  "all",
  # Excluded — exported-identifier renames (banner.Result.Http/Https,
  # discovery.Host.Ip/DnsName, webenum.Result.Url/Html, IsValidIpV*,
  # ByIp, UrlToRelative, ...) are a breaking change for the LSD consumer
  # and must go through the Cross-Product Dependency workflow.
  "-ST1003",
]
